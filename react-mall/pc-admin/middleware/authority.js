const { authorityValidate } = require('../utils/jwt');
const Router = require('koa-router');
const router = new Router();

module.exports = () => {
   /* router.get('/admin', async (ctx, next) => {
        authorityValidate(ctx, next);
    });*/
};
